pipeline {
	agent any
	dir('packages/augur-core')
	{
		stages {
			stage('Build') {
				steps {
					sh 'pwd'
				}
			}
			stage('Dependency Installation') {
				steps {
				}
			}
			stage('Test') {
				steps {
					echo 'Testing ..'
				}
			}
			stage('Deploy') {
				steps {
					echo 'Deploying ..'
				}
			}
		}
	}
}
