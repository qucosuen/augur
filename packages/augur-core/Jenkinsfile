pipeline {
	agent any

	stages {
		stage('Build') {
			steps {
				sh "$PWD"
				dir('packages/augur-core') {
					sh "$PWD"
				}
				sh "$PWD"
			}
		}
		stage('Test') {
			steps {
				echo 'Testing ..'
			}
		}
		stage('Deploy') {
			steps {
				echo 'Deploying ..'
			}
		}
	}
}
